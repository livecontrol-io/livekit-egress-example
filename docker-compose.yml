version: '3'

services:
  livekit:
    image: livekit/livekit-server:vfix-node-ip-SNAPSHOT
    restart: always
    ports:
      - '7880:7880'
      - '7881:7881'
      - '7882:7882/udp'
    expose:
      - 7880
      - 7881
      - '7882/udp'
      - '50000-60000/udp'
    volumes:
      - './livekit.yaml:/livekit.yaml'
    command: "--config /livekit.yaml --dev --node-ip 127.0.0.1"
  frontend:
    build:
      context: ./packages/frontend
    depends_on:
      - backend
      - production
    ports:
      - "3000:3000"
    volumes:
      - ./packages/frontend/src:/app/src
    environment:
      - LC_BACKEND_PATH=http://backend:8082
      - LC_PRODUCTION_PATH=http://production:8083
      - LC_CONTAINER=true
  backend:
    build:
      context: ./packages/backend
    depends_on:
      - livekit
    ports:
      - "8082:8082"
    volumes:
      - ./packages/backend/src:/app/src
    env_file:
      - .env
    environment:
      - PORT=8082
  production:
    build:
      context: ./packages/production
    depends_on:
      - livekit
    ports:
      - "8083:8083"
    volumes:
      - ./packages/production/src:/app/src
    env_file:
      - .env
    environment:
      - PORT=8083
